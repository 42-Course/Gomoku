rd_("BbAI: search + heuristic evaluation.AhCandidate move position.ClBitwise OR with another bitboard. Used to compute \xe2\x80\x9cany \xe2\x80\xa6CjCompact board position as a linear index (y * 19 + x). \xe2\x80\xa6Dk<code>player</code> has won, either by five-in-a-row or by five capture \xe2\x80\xa6DgAdd the counts in <code>rhs</code> to <code>self</code> field-by-field.BcLook up a position by Zobrist hash.CcReturns whether the bit at <code>idx</code> is set.CaCheck if the given coordinate contains the playerFbIndex into <code>[BitBoard; 2]</code> arrays \xe2\x80\x94 Black is <code>0</code>, White is <code>1</code>.AmReturns the underlying index.BoInitialize all Zobrist keys using a fixed seed.DdCreate a transposition table with <code>2^size_power</code> entries.AiCreate an empty bitboard.AfCreate an empty board.CdStart a new game with an empty board, Black to move.BdPosition where the stone was placed.B`Set the bit at <code>idx</code>.DjOptional stone at a board cell: <code>Some(player)</code> if occupied, \xe2\x80\xa6AmBoard is full with no winner.BdThe complete state of a Gomoku game.BbA single move in the game history.iNo value.BbSome value of type <code>T</code>.CkHeuristic evaluation from the side-to-move\xe2\x80\x99s perspective.AoReturns the argument unchanged.00000000000000CnGomoku rules: turns, captures, win/draw detection, and the \xe2\x80\xa6DiPlace the current player\xe2\x80\x99s stone at <code>Pos</code> and update the \xe2\x80\xa6ClZobrist hash of the current game state (board, captures, \xe2\x80\xa6BaCalls <code>U::from(self)</code>.00000000000000DbConvenience wrapper around <code>Game::play_move</code> for tests.AfKeys for side-to-move.CeFirst player. Conventionally drawn as <code>X</code>.CjThe full game board: one <code>BitBoard</code> per player.C`Type of score stored in the transposition table.CfSecond player. Conventionally drawn as <code>O</code>.BlBit-packed board storage and line iteration.BaKeys for each (position, player).AaStone placements.BbClear the bit at <code>idx</code>.CgThe unit <code>(dx, dy)</code> step for this direction.CbSentinel empty entry used to initialize the table.AkRuns of 5 or more in a row.CoMap <code>(x, y)</code> to a flat bit index in row-major order.CaScore from the root side-to-move\xe2\x80\x99s perspective.BeOrdering score used for move sorting.AmConverts the index to (x, y).DhWhich side a stone belongs to. Black moves first (index <code>0</code>).mGomoku engineAmInsert or replace a TT entry.ChNegamax + alpha-beta search with transposition table \xe2\x80\xa6De<code>Ongoing</code>, <code>Win(player)</code>, or <code>Draw</code>.AeMore moves are legal.CkOne cached search result stored in the transposition table.ClGlobal Zobrist table, initialized once and shared across \xe2\x80\xa6BiZobrist key table for hashing game state.BaKeys for (player, capture count).BnLook up the contents of cell <code>Pos</code>.nTable storage.CjPlayed moves in order. Length determines whose move it is.CmWhether every cell on the board is occupied (used to flag \xe2\x80\xa6CkNegamax with alpha-beta pruning and transposition table \xe2\x80\xa6CcZobrist hashing for fast game state identification.CnOne player\xe2\x80\x99s stones, stored as a bitmap with one bit per \xe2\x80\xa6Ca<code>(1, 1)</code> \xe2\x80\x94 top-left to bottom-right.Bf<code>(0, 1)</code> \xe2\x80\x94 top to bottom.CeCoordinates of opponent stones captured by this move.DdCapture pair counts as <code>(black, white)</code>. Five pairs wins.CiScore the position from the side-to-move\xe2\x80\x99s perspective.CoWhether <code>(x, y)</code> is empty for <em>both</em> players.Bf2-runs with empty cells on both sides.AaThe other player.CbBitwise pattern detection on a single packed line.DhPlace the current player\xe2\x80\x99s stone at <code>(x, y)</code> and update \xe2\x80\xa6ClPseudo-random 64-bit generator (SplitMix-like), used for \xe2\x80\xa6CdOne of the four directions a Gomoku line can run in.CkScore returned for a winning terminal position. Mirrors \xe2\x80\xa6CjScore returned for a decisive terminal position. Large \xe2\x80\xa6C`Run alpha-beta and return the best move + score.DjThe chosen move, or <code>None</code> at depth 0 / when no legal moves \xe2\x80\xa6DkWhether the stone at <code>(x, y)</code> participates in a 5+ run along \xe2\x80\xa6AiBoard geometry constants.BaToggles side-to-move in the hash.Bf4-runs with empty cells on both sides.CbPack one straight line of cells into two bitmasks.CjPlace the current player\xe2\x80\x99s stone at <code>(x, y)</code>.CiFixed-size transposition table used by the alpha-beta \xe2\x80\xa6CkReverse the most recent move, restoring captured stones \xe2\x80\xa6DcNumber of cells on each edge of the board, as a <code>usize</code>.DkTotal number of cells on the board (<code>BOARD_SIZE * BOARD_SIZE</code>\xe2\x80\xa6ClWhether the game is still being played, has been won, or \xe2\x80\xa6Bf<code>(1, 0)</code> \xe2\x80\x94 left to right.C`A move paired with its ordering heuristic score.CaLook up the contents of cell <code>(x, y)</code>.Am2-runs with one side blocked.EhToggles hash for placing/removing a stone at <code>pos</code> for <code>player</code>\xe2\x80\xa6Bf3-runs with empty cells on both sides.F`Bits set at position <code>i</code> iff a maximal run of <code>me</code> of <em>exactly</em> \xe2\x80\xa6Am4-runs with one side blocked.CnValidate that <code>(x, y)</code> is a legal placement target.CcSame as <code>hash_place</code> (XOR is symmetric).BoWhether cell <code>(x, y)</code> holds a stone.CeScore and sort candidate moves for alpha-beta search.CdMark cell <code>(x, y)</code> as containing a stone.CmPlace a stone for <code>player</code> at <code>(x, y)</code>.CaPrint the board to stdout in human-readable form.CmPrint the board (and optionally a header showing the move \xe2\x80\xa6Cb<code>(1, -1)</code> \xe2\x80\x94 bottom-left to top-right.DdNumber of cells on each edge of the board, as an <code>isize</code>.BfWhat the search returns to the caller.EcCapture pairs <code>me</code> is ahead by. Negative when <code>me</code> is behind.BnSmall positional bonus favoring central moves.Am3-runs with one side blocked.CnToggles hash for a given capture count of <code>player</code>.BlClear the stone at cell <code>(x, y)</code>.EaRemove <code>player</code>\xe2\x80\x99s stone at <code>(x, y)</code>. Used by undo and \xe2\x80\xa6CkTotal pattern score for one player across every line on \xe2\x80\xa6B`Bonus applied per captured pair.CmA single 5-in-a-row already wins, but we never reach this \xe2\x80\xa6CkTally of distinct max-length runs found on one line for \xe2\x80\xa6CjLarge bonus forcing the TT best move to be searched first.CeNumber of capture pairs taken by <code>player</code>.AhVisit every distinct \xe2\x80\xa6DiDoes the just-placed stone at <code>(x, y)</code> create a free-three \xe2\x80\xa6DhWhether the current player could legally play at <code>(x, y)</code> \xe2\x80\xa6CgMove ordering heuristics for negamax alpha-beta search.CiTotal nodes (including leaves) visited during the search.BlAll four direction deltas, in a fixed order.DkResolve captures triggered by a stone just played at <code>(x, y)</code>\xe2\x80\xa6ChWalk one packed line and tally every distinct run by \xe2\x80\xa6DdWhose turn it currently is. Derived from <code>history.len()</code>.CnCandidate moves the search should consider, in unspecified \xe2\x80\xa6EdDoes this packed line contain <em>any</em> free-three pattern for <code>me</code>\xe2\x80\xa6DaWhose move is the <code>move_index</code>-th move (zero-indexed).CjIf the position is terminal, return its score from the \xe2\x80\xa6DfChebyshev radius used by <code>Game::generate_moves</code> to grow \xe2\x80\xa6EhEstimate the tactical strength of playing at <code>pos</code> for <code>player</code>\xe2\x80\xa6DbPack a small local line around <code>pos</code> into bit patterns.CmHow many of the four directions show a free three through \xe2\x80\xa6DhConvert a <code>PatternCounts</code> tally to an integer score using theCkFixed-size transposition table used to cache previously \xe2\x80\xa6BnBonus for moves that immediately win the game.")