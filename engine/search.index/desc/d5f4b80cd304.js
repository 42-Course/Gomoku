rd_("BbColumn where the stone was placed.AoRow where the stone was placed.BbAI: search + heuristic evaluation.CnThe move that led to this node. <code>None</code> at the root.ClBitwise OR with another bitboard. Used to compute \xe2\x80\x9cany \xe2\x80\xa6Dk<code>player</code> has won, either by five-in-a-row or by five capture \xe2\x80\xa6DgAdd the counts in <code>rhs</code> to <code>self</code> field-by-field.CaCheck if the given coordinate contains the playerFbIndex into <code>[BitBoard; 2]</code> arrays \xe2\x80\x94 Black is <code>0</code>, White is <code>1</code>.CbCreate an empty observer ready to record a search.AiCreate an empty bitboard.AfCreate an empty board.CdStart a new game with an empty board, Black to move.DjOptional stone at a board cell: <code>Some(player)</code> if occupied, \xe2\x80\xa6AmBoard is full with no winner.BdThe complete state of a Gomoku game.BbA single move in the game history.iNo value.BbSome value of type <code>T</code>.CkHeuristic evaluation from the side-to-move\xe2\x80\x99s perspective.AoReturns the argument unchanged.00000000000CnGomoku rules: turns, captures, win/draw detection, and the \xe2\x80\xa6BaCalls <code>U::from(self)</code>.00000000000DbConvenience wrapper around <code>Game::play_move</code> for tests.CeFirst player. Conventionally drawn as <code>X</code>.CjThe full game board: one <code>BitBoard</code> per player.CfSecond player. Conventionally drawn as <code>O</code>.BlBit-packed board storage and line iteration.AaStone placements.CgThe unit <code>(dx, dy)</code> step for this direction.BdCalled once before exploring a node.AkRuns of 5 or more in a row.CoMap <code>(x, y)</code> to a flat bit index in row-major order.EaCalled once after a node returns. <code>pruned</code> is <code>true</code> if \xe2\x80\xa6CaScore from the root side-to-move\xe2\x80\x99s perspective.CgFinal score returned by this node, side-to-move\xe2\x80\x99s \xe2\x80\xa6DhWhich side a stone belongs to. Black moves first (index <code>0</code>).mGomoku engineDb<code>true</code> if an alpha-beta cutoff stopped expansion early.AlNegamax + alpha-beta search.De<code>Ongoing</code>, <code>Win(player)</code>, or <code>Draw</code>.AeMore moves are legal.AdBeta bound on entry.CaLook up the contents of cell <code>(x, y)</code>.CjPlayed moves in order. Length determines whose move it is.CmWhether every cell on the board is occupied (used to flag \xe2\x80\xa6BdNegamax with alpha-beta. Returns \xe2\x80\xa6CnOne player\xe2\x80\x99s stones, stored as a bitmap with one bit per \xe2\x80\xa6Ca<code>(1, 1)</code> \xe2\x80\x94 top-left to bottom-right.CjHook points the search calls on entering/leaving every \xe2\x80\xa6Bf<code>(0, 1)</code> \xe2\x80\x94 top to bottom.AeAlpha bound on entry.CeCoordinates of opponent stones captured by this move.DdCapture pair counts as <code>(black, white)</code>. Five pairs wins.CbRecorded children, in the order they were visited.CiScore the position from the side-to-move\xe2\x80\x99s perspective.CoWhether <code>(x, y)</code> is empty for <em>both</em> players.Bf2-runs with empty cells on both sides.AaThe other player.CbBitwise pattern detection on a single packed line.CdOne of the four directions a Gomoku line can run in.CkScore returned for a winning terminal position. Mirrors \xe2\x80\xa6CjScore returned for a decisive terminal position. Large \xe2\x80\xa6CkFast path: run alpha-beta and return the best move + score.DjThe chosen move, or <code>None</code> at depth 0 / when no legal moves \xe2\x80\xa6DkWhether the stone at <code>(x, y)</code> participates in a 5+ run along \xe2\x80\xa6AiBoard geometry constants.CkConsume the observer and return the recorded root (if any).Bf4-runs with empty cells on both sides.CbPack one straight line of cells into two bitmasks.DhPlace the current player\xe2\x80\x99s stone at <code>(x, y)</code> and update \xe2\x80\xa6CkReverse the most recent move, restoring captured stones \xe2\x80\xa6DcNumber of cells on each edge of the board, as a <code>usize</code>.DkTotal number of cells on the board (<code>BOARD_SIZE * BOARD_SIZE</code>\xe2\x80\xa6ClWhether the game is still being played, has been won, or \xe2\x80\xa6Bf<code>(1, 0)</code> \xe2\x80\x94 left to right.CiOne node in the recorded search tree (verbose mode only).DdHow many <code>u64</code> words are needed to hold one bit per cell.Am2-runs with one side blocked.Bf3-runs with empty cells on both sides.F`Bits set at position <code>i</code> iff a maximal run of <code>me</code> of <em>exactly</em> \xe2\x80\xa6Am4-runs with one side blocked.CnValidate that <code>(x, y)</code> is a legal placement target.BoWhether cell <code>(x, y)</code> holds a stone.CdMark cell <code>(x, y)</code> as containing a stone.CmPlace a stone for <code>player</code> at <code>(x, y)</code>.CaPrint the board to stdout in human-readable form.CmPrint the board (and optionally a header showing the move \xe2\x80\xa6Cb<code>(1, -1)</code> \xe2\x80\x94 bottom-left to top-right.DdNumber of cells on each edge of the board, as an <code>isize</code>.DjZero-overhead observer. Monomorphization + <code>#[inline]</code> lets \xe2\x80\xa6BfWhat the search returns to the caller.CgBuilds a tree of every visited node for the visualizer.EcCapture pairs <code>me</code> is ahead by. Negative when <code>me</code> is behind.Am3-runs with one side blocked.BlClear the stone at cell <code>(x, y)</code>.EaRemove <code>player</code>\xe2\x80\x99s stone at <code>(x, y)</code>. Used by undo and \xe2\x80\xa6CkTotal pattern score for one player across every line on \xe2\x80\xa6CmA single 5-in-a-row already wins, but we never reach this \xe2\x80\xa6CkTally of distinct max-length runs found on one line for \xe2\x80\xa6AhVisit every distinct \xe2\x80\xa6DiDoes the just-placed stone at <code>(x, y)</code> create a free-three \xe2\x80\xa6DhWhether the current player could legally play at <code>(x, y)</code> \xe2\x80\xa6CiTotal nodes (including leaves) visited during the search.BlAll four direction deltas, in a fixed order.DkResolve captures triggered by a stone just played at <code>(x, y)</code>\xe2\x80\xa6ChWalk one packed line and tally every distinct run by \xe2\x80\xa6DdWhose turn it currently is. Derived from <code>history.len()</code>.CnCandidate moves the search should consider, in unspecified \xe2\x80\xa6EdDoes this packed line contain <em>any</em> free-three pattern for <code>me</code>\xe2\x80\xa6DaWhose move is the <code>move_index</code>-th move (zero-indexed).AnWhose turn it is at this node.CjIf the position is terminal, return its score from the \xe2\x80\xa6DfChebyshev radius used by <code>Game::generate_moves</code> to grow \xe2\x80\xa6BfPlies still to expand below this node.CkVerbose path: same search, plus a recorded tree for the \xe2\x80\xa6CmHow many of the four directions show a free three through \xe2\x80\xa6DhConvert a <code>PatternCounts</code> tally to an integer score using the")